# Contracts (zod)

Shared request/response schemas and types for public and admin endpoints.

Import from `@salon/contracts` and use the zod schemas to parse/validate incoming data and infer TS types.